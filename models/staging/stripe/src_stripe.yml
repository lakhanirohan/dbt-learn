version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        freshness: 
          error_after:
            count: 48
            period: hour
          warn_after:
            count: 48
            period: hour
        loaded_at_field: _batched_at
        